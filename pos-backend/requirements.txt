# Web framework and server
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Validation
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# Reference data (ISO 3166 countries, ISO 4217 currencies)
pycountry==24.6.1

# Authentication
python-jose[cryptography]==3.3.0
argon2-cffi==23.1.0

# Task queue
celery==5.4.0
redis==5.2.1

# Logging
structlog==24.4.0
colorama==0.4.6

# HTTP client (for tests) — supabase requires httpx>=0.26,<0.29
httpx>=0.26,<0.29

# Email
resend==2.4.0

# File upload / storage
supabase==2.10.0
python-multipart==0.0.20
Pillow==11.0.0

# Bulk import/export (Stage 19)
openpyxl==3.1.5

# Invoice PDF export (Stage 21) — requires system Pango/Cairo/GDK-Pixbuf libs,
# see .github/workflows/ci.yml for the apt packages this needs at runtime
weasyprint==63.1

# CLI
typer==0.15.1

# Environment
python-dotenv==1.0.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
