[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
# serve handles SPA routing: -s redirects all unknown paths to index.html
# so React Router works correctly on direct URL loads and refreshes.
startCommand = "npx serve dist -s --listen tcp://0.0.0.0:$PORT"
